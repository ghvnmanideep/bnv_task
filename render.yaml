services:
  - type: web
    name: bnv-portal
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        fromContext: envVar
      - key: FRONTEND_URL
        value: https://bnv-portal.onrender.com
